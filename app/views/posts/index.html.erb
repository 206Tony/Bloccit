<h1>All Posts</h1>
# #12 iterate over each post in @posts. for ea. post create a link w/ post.title as the text that links to /posts/id, with id from post.id.
<% @posts.each do |post| %>
	<div class="media">
		<div class="media-body">
			<h4 class="media-heading">
				<%= link_to post.title, post %>
			</h4>
		</div>
	</div>
<% end %>
